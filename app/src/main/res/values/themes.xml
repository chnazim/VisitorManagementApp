<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VisitorManagementApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondaryDark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>

        <!-- Background color -->
        <item name="backgroundColor">@color/background_primary</item>

        <!-- Text colors -->
        <item name="textFillColor">@color/text_primary</item> <!-- Light primary text color -->
        <item name="android:textColorSecondary">@color/text_secondary
        </item> <!-- Light secondary text color -->
        <item name="colorSurface">@color/background_secondary</item>

        <!-- Toolbar -->
        <item name="android:textColorPrimary">@color/text_primary</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.VisitorManagementApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>